// Superseded by scripts/render-media.ts which renders the social preview
// alongside the README banners. Kept as a placeholder since `rm` is unavailable
// from this environment; safe to `git rm scripts/social-preview.ts`.
export {};
